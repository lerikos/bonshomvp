<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "BONSHO", current_user ? '/alerts-listing' : root_path, class: "navbar_brand", id: "logo" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if action_name == 'home' %>
          <li><a href="#" class='move-to-about'>About</a></li>
        <% else %>
          <li><%= link_to "Alerts", '/alerts-listing' %></li>
          <li><%= link_to "Evailable Alerts", '/all-alerts' %></li>
          <li><%= link_to "Subscribe on alerts", '/subscribe' %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="/profile">Profile</a></li>
              <% if current_user.admin %>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Alerts', alerts_path %></li>
                <li><%= link_to 'Products', products_path %></li>
                <li><%= link_to 'Tags', tags_path %></li>
              <% end %>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'Log in', new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
